test_manager.publish = function() {
  popup.show(test_manager.publish_popup, function() {
    // var max_points = editor.test_data.calculate_max_points();
    // popup.$.find(".__max_ponts").text(max_points);
  }, {'width': '64rem'});
}
